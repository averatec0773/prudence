"""Store: Prudence's own record.

Today only repository identity lives here. Later: the raw archive (bytes kept
unmodified) and the derived tables (rebuildable from the archive, each carrying the
version of the code that produced it).
"""
