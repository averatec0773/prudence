"""The Claude Code MCP server: the store, read-only, over stdio.

`server.py` is the only module here, and the only place in Prudence that imports the
`mcp` package. See `prudence.cli.mcp` for the command that runs it.
"""
