{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The panel and the window talk to the shell through the bridge commands only.",
  "windows": [
    "panel",
    "main"
  ],
  "permissions": [
    "core:default",
    "positioner:default"
  ]
}
