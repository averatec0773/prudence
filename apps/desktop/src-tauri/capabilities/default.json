{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The panel window talks to the shell through the bridge commands only.",
  "windows": [
    "panel"
  ],
  "permissions": [
    "core:default",
    "positioner:default"
  ]
}
