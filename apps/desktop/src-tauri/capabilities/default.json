{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the two pages are allowed to ask of the shell.",
  "windows": ["panel", "main"],
  "permissions": []
}
