# Generated by `Scripts/bootstrap.sh` from project.yml; never edited, never tracked.
Prudence.xcodeproj/

# Build output, from xcodebuild and from swift build.
build/
DerivedData/
.build/
Render/.build/
PrudenceKit/.build/

# Written by `Scripts/shots.sh`; uploaded by CI as an artifact instead of being tracked.
shots/

# Copied in by `Scripts/shots.sh` so the harness can render the shipping views.
Render/Sources/PrudenceRender/Views/
